{
  "name": "hris-backend",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd backend && npm ci",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
